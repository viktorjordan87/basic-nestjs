# Redis Configuration with ACL
# Disable default user
user default off

# Create user with username and password
# Format: user <username> on ><password> ~<key-pattern> +<command-category>
user redisuser on >redispassword ~* +@all

# Save configuration
save 20 1

# Log level
loglevel warning
